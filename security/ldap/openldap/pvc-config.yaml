apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openldap-config
  namespace: ldap
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
