apiVersion: v1
kind: Secret
metadata:
  name: auth-gateway-edge
  namespace: kamiwaza
type: Opaque
stringData:
  # Must match core-values-snippet.yaml -> network.traefik.extraObjects[*].spec.headers.customRequestHeaders.X-Edge-Auth
  secret: "REPLACE_WITH_EDGE_SHARED_SECRET"
