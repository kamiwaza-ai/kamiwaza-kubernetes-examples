apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The published quickstart intent is what this environment validates, so it is
# a base here rather than a manual apply step. Only the environment-owned
# statements are patched.
resources: [gateway.yaml, ../../../quickstart]
patches: [{ path: platform.yaml }]
